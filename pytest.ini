# pytest.ini  – keep pytest out of third-party packages
[pytest]
testpaths      = tests           # only collect tests/ …
norecursedirs  = .* venv .venv env AutoGPTQ build dist _build
addopts        = -q
